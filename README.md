# proofpoint_itm
Proofpoint ITM API client library for python

## Installation
```
python -m pip install proofpoint_itm
```

## Usage
```
itm_client = ITMClient(
    <itm console url>,
    <api client id>,
    <api client secret>)
```
